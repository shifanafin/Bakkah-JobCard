// Attendance is now handled manually via AttendanceWidget in the sidebar.
// Employees check in/out via the button — no automatic WiFi tracking.
export default function AttendanceTracker() {
  return null
}
